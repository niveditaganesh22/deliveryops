name: validate-blueprint

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ensure workflows exist
        run: |
          test -f .github/workflows/node-ci.yml
          test -f .github/workflows/terraform-plan.yml
          echo "Blueprint structure looks OK."